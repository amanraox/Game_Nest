import React from 'react';
import ReactDOM from 'react-dom/client';
import './Touchingindex.css';
import Touching from './Touching';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Touching/>
  </React.StrictMode>
 
);